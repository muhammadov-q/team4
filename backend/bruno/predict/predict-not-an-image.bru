meta {
  name: Predict - not an image
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/predict
  body: multipartForm
  auth: none
}

body:multipart-form {
  image: @file(fixtures/not-an-image.txt)
}

assert {
  res.status: eq 415
}

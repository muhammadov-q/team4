meta {
  name: Predict - image
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/predict
  body: multipartForm
  auth: none
}

body:multipart-form {
  image: @file(fixtures/test.png)
}

assert {
  res.status: eq 200
  res.body.prediction: eq 250000
  res.body.model_version: eq mock
}
